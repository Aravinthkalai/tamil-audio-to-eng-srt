name: tamil_srt_app
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: "^3.13.2"

dependencies:
  flutter:
    sdk: flutter

  file_picker: ^10.3.6
  path_provider: ^2.1.3
  cupertino_icons: ^1.0.8
  whisper_ggml: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
